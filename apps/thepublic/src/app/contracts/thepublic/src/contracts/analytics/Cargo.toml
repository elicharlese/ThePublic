[package]
name = "analytics"
version = "0.1.0"
edition = "2018"

[dependencies]
near-sdk = "3.1.0"   # Ensure you have the necessary version
serde = "1.0"
serde_json = "1.0"

[lib]
crate-type = ["cdylib", "rlib"]